androidのアプリをバックグラウンドで動作させてみるテストその２
サービス登録編

アプリのバックグラウンドでGPSを取得したり時間を測定するため、UIが表示されていなくても処理を行うためのサービス実装のテスト

var 1.00
TestServiceクラスを作り、Manifestに登録、startService()で実行したのちUIを閉じてもトーストが飛んで切るか確認するもの。
　　サービスが実行されるのは確認できた。
　　　　問題点１：UI側がスタックする、おそらく同じスレッド？をサービスが継続して使っているから、サービスが終わるまでUIに帰ってこないため？
　　　　　　　　　だとすると、非同期で実行する必要ががあるが、非同期でサービスを使用すると。
　　　　　　　　　UI(親)⇒非同期(子)in{サービス}となって、それってサービスなの？ってならないだろうか？
　　　　問題点２：UIとじるとサービスも終了してるきがする、サービスってバックグラウンドで動く機能ではないのだろうか？
